# WASM examples written in Rust

## Build

1. Install the WASM toolchain:
```sh
rustup target add wasm32-unknown-unknown
```
2. Run the [build command](./build). This will install all the `.wasm` files
   into the `bin` directory.
```sh
bash build
```
