[package]
name = "walc"
version = "0.1.0"
edition = "2024"
authors = ["Mark Lagodych"]
license = "MIT"
description = "WebAssembly to Lambda Calculus compiler"

[dependencies.wasmbin]
version = "^0.8"
features = [] # Ignore validation, SIMD, and WASM components

[dependencies.clap]
version = "^4.5"
features = ["derive"]

[features]
unbound-unreachable = []
